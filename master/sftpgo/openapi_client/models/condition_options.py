# -*- coding: utf-8 -*-

"""
    SFTPGo

    SFTPGo allows you to securely share your files over SFTP and optionally over HTTP/S, FTP/S and WebDAV as well. Several storage backends are supported and they are configurable per-user, so you can serve a local directory for a user and an S3 bucket (or part of it) for another one. SFTPGo also supports virtual folders, a virtual folder can use any of the supported storage backends. So you can have, for example, a user with the S3 backend mapping a Google Cloud Storage bucket (or part of it) on a specified path and an encrypted local filesystem on another one. Virtual folders can be private or shared among multiple users, for shared virtual folders you can define different quota limits for each user. SFTPGo supports groups to simplify the administration of multiple accounts by letting you assign settings once to a group, instead of multiple times to each individual user. The SFTPGo WebClient allows end users to change their credentials, browse and manage their files in the browser and setup two-factor authentication which works with Authy, Google Authenticator and other compatible apps. From the WebClient each authorized user can also create HTTP/S links to externally share files and folders securely, by setting limits to the number of downloads/uploads, protecting the share with a password, limiting access by source IP address, setting an automatic expiration date.

    The version of the OpenAPI document: 2.5.6
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import List, Optional
from pydantic import (
    BaseModel,
    Field,
    StrictBool,
    StrictInt,
    StrictStr,
    conlist,
    validator,
)
from openapi_client.models.condition_pattern import ConditionPattern


class ConditionOptions(BaseModel):
    """
    ConditionOptions
    """

    names: Optional[conlist(ConditionPattern)] = None
    group_names: Optional[conlist(ConditionPattern)] = None
    role_names: Optional[conlist(ConditionPattern)] = None
    fs_paths: Optional[conlist(ConditionPattern)] = None
    protocols: Optional[conlist(StrictStr)] = None
    provider_objects: Optional[conlist(StrictStr)] = None
    min_size: Optional[StrictInt] = None
    max_size: Optional[StrictInt] = None
    concurrent_execution: Optional[StrictBool] = Field(
        None, description="allow concurrent execution from multiple nodes"
    )
    __properties = [
        "names",
        "group_names",
        "role_names",
        "fs_paths",
        "protocols",
        "provider_objects",
        "min_size",
        "max_size",
        "concurrent_execution",
    ]

    @validator("protocols")
    def protocols_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in (
                "SFTP",
                "SCP",
                "SSH",
                "FTP",
                "DAV",
                "HTTP",
                "HTTPShare",
                "OIDC",
            ):
                raise ValueError(
                    "each list item must be one of ('SFTP', 'SCP', 'SSH', 'FTP', 'DAV', 'HTTP', 'HTTPShare', 'OIDC')"
                )
        return value

    @validator("provider_objects")
    def provider_objects_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in (
                "user",
                "group",
                "admin",
                "api_key",
                "share",
                "event_action",
                "event_rule",
            ):
                raise ValueError(
                    "each list item must be one of ('user', 'group', 'admin', 'api_key', 'share', 'event_action', 'event_rule')"
                )
        return value

    class Config:
        """Pydantic configuration"""

        allow_population_by_field_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> ConditionOptions:
        """Create an instance of ConditionOptions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True, exclude={}, exclude_none=True)
        # override the default output from pydantic by calling `to_dict()` of each item in names (list)
        _items = []
        if self.names:
            for _item in self.names:
                if _item:
                    _items.append(_item.to_dict())
            _dict["names"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in group_names (list)
        _items = []
        if self.group_names:
            for _item in self.group_names:
                if _item:
                    _items.append(_item.to_dict())
            _dict["group_names"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in role_names (list)
        _items = []
        if self.role_names:
            for _item in self.role_names:
                if _item:
                    _items.append(_item.to_dict())
            _dict["role_names"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in fs_paths (list)
        _items = []
        if self.fs_paths:
            for _item in self.fs_paths:
                if _item:
                    _items.append(_item.to_dict())
            _dict["fs_paths"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> ConditionOptions:
        """Create an instance of ConditionOptions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return ConditionOptions.parse_obj(obj)

        _obj = ConditionOptions.parse_obj(
            {
                "names": [
                    ConditionPattern.from_dict(_item) for _item in obj.get("names")
                ]
                if obj.get("names") is not None
                else None,
                "group_names": [
                    ConditionPattern.from_dict(_item)
                    for _item in obj.get("group_names")
                ]
                if obj.get("group_names") is not None
                else None,
                "role_names": [
                    ConditionPattern.from_dict(_item) for _item in obj.get("role_names")
                ]
                if obj.get("role_names") is not None
                else None,
                "fs_paths": [
                    ConditionPattern.from_dict(_item) for _item in obj.get("fs_paths")
                ]
                if obj.get("fs_paths") is not None
                else None,
                "protocols": obj.get("protocols"),
                "provider_objects": obj.get("provider_objects"),
                "min_size": obj.get("min_size"),
                "max_size": obj.get("max_size"),
                "concurrent_execution": obj.get("concurrent_execution"),
            }
        )
        return _obj
